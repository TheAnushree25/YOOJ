# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment
.env
.env.*
!.env.example

# Build metadata
*.tsbuildinfo
.cache
.temp

# OS
Thumbs.db
ehthumbs.db

# The investor deck: deck/sealed (the slides, AES-256-GCM ciphertext from
# scripts/deck/seal.mjs) and deck/fonts (the watermark font, OFL) ARE committed,
# so the host builds with them. Unreadable without DECK_KEY, which lives only in
# .env.local and the host's environment. The PDF itself never enters the repo.
